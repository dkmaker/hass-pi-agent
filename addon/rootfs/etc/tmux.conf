# Enable extended keys in csi-u format (required by Pi)
set -g extended-keys on
set -g extended-keys-format csi-u

# Enable mouse support (scroll, select panes/windows)
set -g mouse on

# Increase scrollback buffer
set -g history-limit 10000
